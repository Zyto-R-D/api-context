{
  "name": "@zyto/api-context",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"lint ok\"",
    "test": "node -e \"process.exit(0)\""
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
