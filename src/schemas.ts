// Placeholder for runtime validation (future)
// import { z } from 'zod'

/** RunRequest schema (for Zod, if added later) */
// export const RunRequestSchema = z.object({
//   prompt: z.string().min(1)
// })

/** PlanStep schema */
// export const PlanStepSchema = z.object({
//   tool: z.enum(['zapier', 'n8n']),
//   action: z.string(),
//   input: z.unknown()
// })
